# To set up openebs use the following command
kubectl apply -f openebs-operator.yaml

# To clean up the openebs entirely use this command(Do not une unless you want to clean up the deployment)
kubectl delete -f openebs-operator.yaml